package it.gov.pagopa.wallet.enums;

public enum SyncTrxStatus {
    CREATED,
    IDENTIFIED,
    AUTHORIZATION_REQUESTED,
    AUTHORIZED,
    CAPTURED,
    REWARDED,
    REJECTED,
    CANCELLED,
    EXPIRED,
    REFUNDED
}
